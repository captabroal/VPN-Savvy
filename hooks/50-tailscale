#!/bin/bash
# Reapply UDP GRO forwarding settings after the network becomes routable.
NETDEV=$(ip route show default | awk '{print $5}' | head -1)
if [ -n "$NETDEV" ]; then
  ethtool -K "$NETDEV" rx-udp-gro-forwarding on rx-gro-list off 2>/dev/null || true
fi
